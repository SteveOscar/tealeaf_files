#The code below will print out the array elements on new lines, as the 'puts' command 
#includes a line break. The .uniq method did not mutate the caller. 

numbers = [1, 2, 2, 3]
numbers.uniq

puts numbers